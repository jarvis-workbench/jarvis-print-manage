name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish a GitHub release"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-latest
          - windows-latest
    runs-on: ${{ matrix.os }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      CSC_IDENTITY_AUTO_DISCOVERY: false
      ELECTRON_MIRROR: https://github.com/electron/electron/releases/download/
      ELECTRON_BUILDER_BINARIES_MIRROR: https://github.com/electron-userland/electron-builder-binaries/releases/download/
      npm_config_electron_mirror: https://github.com/electron/electron/releases/download/
      npm_config_electron_builder_binaries_mirror: https://github.com/electron-userland/electron-builder-binaries/releases/download/
      NPM_CONFIG_ELECTRON_MIRROR: https://github.com/electron/electron/releases/download/
      NPM_CONFIG_ELECTRON_BUILDER_BINARIES_MIRROR: https://github.com/electron-userland/electron-builder-binaries/releases/download/
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Configure Electron download mirrors
        run: |
          npm config set electron_mirror https://github.com/electron/electron/releases/download/ --location=project
          npm config set electron_builder_binaries_mirror https://github.com/electron-userland/electron-builder-binaries/releases/download/ --location=project

      - name: Build web assets
        run: npm run build:web

      - name: Package
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" || "${{ github.event.inputs.publish }}" == "true" ]]; then
            npx electron-builder --publish always
          else
            npx electron-builder --publish never
          fi

      - name: Upload dry-run artifacts
        if: ${{ !(github.ref_type == 'tag' || github.event.inputs.publish == 'true') }}
        uses: actions/upload-artifact@v4
        with:
          name: jarvis-print-manage-${{ matrix.os }}
          path: release/**
