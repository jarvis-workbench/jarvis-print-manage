# 实施前置清单：00003 业务规划（驱动传输 + 对标能力）

更新时间：2026-03-28
状态：评审中
关联主规划：`docs/planning/00003-lan-driver-transfer-and-hiprint/00001-主规划.md`

## 1. 范围冻结
- 00003 仅做业务层：驱动传输业务化 + 打印协议对标 + 渲染输出。
- 00003 不改 00002 底座协议结构。

## 2. 必要依赖（进入开发前必须满足）
- 00002 底座已可用：
  - 节点发现
  - 配对鉴权
  - 白名单
  - 任务状态查询
- 00001 归档能力稳定：
  - `.pdrv.zip`
  - `archiveSha256`
  - 安装链路可复用

## 3. 技术依赖
- `fabric` / `gl-matrix` / `html2canvas` 版本选型已冻结。
- Socket 协议 V1 评审通过。
- 打印错误码与日志字段评审通过。

## 4. Gate（防止超范围）
- Gate-A：先通 `news` 与 `success/error` 闭环。
- Gate-B：再通 `render-jpeg/pdf/print` 闭环。
- Gate-C：最后补日志、重打、中转扩展预留。

## 5. 风险前置处理
- 平台差异（Win/Linux/mac）先做能力探测，不硬编码假设。
- 大 payload 先限流再放量，避免先上先崩。
- 任务并发默认 1，压测通过后再放开。
