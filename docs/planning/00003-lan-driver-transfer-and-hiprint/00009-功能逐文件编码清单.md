# 编码清单：00003 逐文件任务拆分

更新时间：2026-03-29
状态：进行中（L1 已完成，L2 待开始）

## 1. 批次规划
- L1：Socket 服务骨架
- L2：四类打印能力
- L3：render 三路能力
- L4：日志与重打
- L5：联调与回归

## 2. 逐文件任务

### 2.1 `electron/main.mjs`
- 注册 `print:*` IPC。
- 接入 `print-socket-service` 生命周期。

### 2.2 `electron/preload.mjs`
- 暴露 `window.eleDrive.printService.*`。

### 2.3 `electron/` 新增文件
- `print-socket-service.mjs`
- `print-task-orchestrator.mjs`
- `worker/print-render-task.mjs`

### 2.4 `src/stores/runtime.js`
- 新增：
  - `printServiceState`
  - `printJobs`
  - `printProtocolVersion`

### 2.5 `src/views/` 新增或扩展
- 打印服务状态区
- 任务列表筛选
- 重打入口

### 2.6 类型与文档
- `src/env.d.ts` 增加 `PrintServiceState/PrintJob`。
- 文档同步更新 00003 契约与里程碑。

## 3. 批次验收
- L1：客户端可连接并获取 `clientInfo/printerList`。
- L2：`html/pdf/url_pdf/blob_pdf` 均可回调成功或失败。
- L3：`render-jpeg/pdf/print` 均可返回结果。
- L4：日志可按 `taskId` 检索，重打可控。
- L5：联调与回归清单全部通过。

## 4. 执行记录（2026-03-29）
- L1 完成项：
  - `electron/main.mjs`：已注册 `print:service:*` IPC，并接入服务启停与状态广播。
  - `electron/preload.mjs` / `electron/preload.cjs`：已暴露打印服务与任务相关桥接 API。
  - `electron/`：已新增 `print-socket-service.mjs`、`print-task-orchestrator.mjs`、`worker/print-render-task.mjs`。
  - `src/stores/runtime.js`：已新增 `printServiceState/printJobs/printProtocolVersion`。
  - `src/env.d.ts`：已新增 `PrintServiceState/PrintJob` 与相关桥接类型。
- L1 待补充验收：
  - 增加 Node demo 脚本并固化到仓库（用于自动验证连接与事件回包）。
