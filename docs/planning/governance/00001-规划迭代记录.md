# 规划迭代记录

更新时间：2026-03-28

## 本轮治理收敛（2026-03-28）
- `docs/planning/` 目录结构调整为：
  - `00001-printer-backup-archive/`
  - `00002-lan-network-sharing/`
  - `governance/`（治理文档，非规划编号）
- 规划编号治理规则收敛：
  - 仅“独立规划”使用 `00001+` 编号目录；
  - 每个规划的扩展文档只允许在本规划目录内连续编号，禁止跨规划穿插编号。
- 关联规划协同治理收敛：
  - 已将“关联规划同步优化与回填”加入评审硬性检查项（`governance/00002-规划评审检查清单.md`）。

## 本轮结论（automation）
- 已读取 `docs/` 记忆文档与 `docs/planning/` 现有规划。
- 确认两份规划方向正确，但执行约束仍偏弱，已补齐“前置依赖/待确认问题/MVP边界/回滚策略”。
- 明确实施顺序：先完成“驱动备份压缩包化”，再进入“局域网组网与驱动共享”。

## 依赖关系
- `局域网组网与驱动共享` 依赖 `打印机驱动备份压缩包化` 提供稳定的传输载体、哈希校验和解压安装流程。
- 若先做局域网共享，后续仍需返工传输协议与安装链路。

## 推荐执行切片
1. 切片 A（P1）：压缩包化备份闭环
2. 切片 B（P2）：局域网只读发现
3. 切片 C（P3）：局域网传输与自动安装

## 当前待确认（阻塞项）
- 压缩格式是否固定为 `.pdrv.zip`。
- 节点鉴权是否固定“一次性配对码 + 白名单缓存”。
- 首版传输是否仅支持 HTTP 明文内网。

## 历次新增（2026-03-28）
- 01/02/99：实施清单 + 前置约束 + 评审 Gate。
- 03/04：P1 编码看板 + P2 模块拆分。
- 05/06：P1 字段IPC契约 + P2/P3 IPC数据契约。
- 07/08：P1 验收执行手册 + P2 白名单日志存储设计。
- 09/10：配对鉴权流程 + 失败重试降级策略。
- 11/12：逐文件编码清单 + 联调发布 Gate。
- 13/14：异常注入测试脚本设计 + 发布回滚演练模板。
- 15/16：测试数据生成规范 + CI 自动化执行顺序。
- 17/18：任务状态机时序图 + 跨版本兼容矩阵。

## 本轮迭代优化（2026-03-28 第九轮）
- 新增 `docs/planning/00002-lan-network-sharing/00014-任务状态机时序图.md`：
  - 固化 `LanTask` 状态集合、状态流转时序、可重试分叉规则与日志回填约束。
- 新增 `docs/planning/00002-lan-network-sharing/00015-跨版本兼容矩阵.md`：
  - 固化 `App/Protocol/Archive` 三层版本门禁、跨版本联调矩阵、错误码映射与 CI 用例映射。

## 本轮迭代优化（2026-03-28 第十轮）
- 新增 `docs/planning/00002-lan-network-sharing/00016-首版默认决策与开关矩阵.md`：
  - 将阻塞项收敛为“首版默认冻结决策”（压缩扩展名、哈希算法、鉴权方式、传输协议、发现范围、nodeId 策略）。
  - 新增功能开关矩阵（`backup/archive`、`lan/discovery`、`lan/transfer`、`lan/autoInstall`）与跳级限制。
  - 固化“偏离默认值必须评审”的变更门禁，避免实现阶段隐式改协议。

## 阻塞项收敛状态（截至 2026-03-28）
- 已由 `19` 文档给出首版默认值，可直接按默认值编码实现。
- 若业务需偏离默认值，按 `99` 的评审模板走变更流程后再实施。

## 下轮迭代入口
- 若进入实现：按 `11 + 17 + 19` 执行 L1，先完成默认值常量与功能开关落盘。
- 若进入联调：按 `12 + 16 + 18` 执行 `CV-01~CV-05`，优先阻断协议/格式不兼容问题。


## 本轮迭代优化（2026-03-28 第十一轮）
- 初版尝试将“对标 electron-hiprint”能力并入 `00002`。
- 经后续边界评审，该轮产出已整体迁移到 `00003-lan-driver-transfer-and-hiprint`，不再作为 `00002` 的有效范围。

## 本轮纠偏（2026-03-28）
- 按用户确认，规划边界重置：
  - `00002-lan-network-sharing` 仅保留“局域网通讯底座”职责。
  - Socket 打印服务与对标 `electron-hiprint` 能力从 `00002` 拆出。
- 新增独立规划：
  - `00003-lan-driver-transfer-and-hiprint`（承接局域网驱动传输与对标能力）。
- 已完成迁移：
  - 对标差距、渲染方案、Socket 协议、实施路线图迁移至 `00003`。

## 本轮推进（2026-03-28）
- 补齐 `00003` 可编码文档链路：
  - `00006-实施前置清单.md`
  - `00007-模块拆分草案.md`
  - `00008-IPC与数据契约草案.md`
  - `00009-功能逐文件编码清单.md`
  - `00010-联调与发布前检查清单.md`
- 修正 `00003` 既有文档中的路径引用，统一指向 `00003` 目录。


## 本轮推进（2026-03-28 第二阶段）
- 继续补齐 `00003` 的 M0 冻结件：
  - `00011-M0协议与错误码冻结清单.md`
  - `00012-M0打印任务状态机与字段冻结.md`
  - `00013-M0-Node最小联通验收规范.md`
- 将 `00003` 从“排期级文档”推进到“可直接进入编码前评审”状态。
