# 编码任务看板：打印机驱动压缩包化（P1）

更新时间：2026-03-28  
状态：可执行（等待开始编码）  
关联实施清单：`docs/planning/01-打印机驱动压缩包化实施清单.md`

## 1. 目标与完成定义
- 目标：将 P1 从“实施清单”转为“可逐项提交的编码任务”。
- 完成定义：
  - `driver-index.json`/`driver-backup.json`/前端类型完成 `archive*` 字段闭环。
  - 备份路径产生 `.pdrv.zip`，安装路径支持“校验 -> 解压 -> 安装 -> 清理”。
  - 兼容旧目录索引，不阻断历史备份恢复。

## 2. 任务拆分（按提交批次）
| 批次 | 任务 | 主要改动点 | 依赖 | 完成判定 |
| --- | --- | --- | --- | --- |
| C1 | 数据结构与兼容兜底 | `electron/main.mjs::normalizeIndex`、`src/env.d.ts` | 无 | 旧索引读取不报错，新增字段可透传 |
| C2 | 备份打包链路 | `backupPrinterDriver` + 压缩/哈希工具函数 | C1 | 备份后可见 `.pdrv.zip` 与 `archive*` 字段 |
| C3 | 安装解压链路 | `installPrinterFromBackup` + 解压/清理工具函数 | C1 | 安装前能校验哈希并自动解压 |
| C4 | 错误码与日志补齐 | 主流程错误分支、任务日志结构 | C2+C3 | 三类错误码可稳定返回 |
| C5 | 回归与验收回填 | 回归脚本/人工测试记录、文档回填 | C1~C4 | 主路径/兼容/异常矩阵打勾完成 |

## 3. 变更清单映射（防漏改）
- 主进程：`electron/main.mjs`
- 脚本签名与脚本目录：`electron/config/script/ps/`
- 渲染层类型：`src/env.d.ts`
- 文档回填：
  - `docs/planning/01-打印机驱动压缩包化实施清单.md`
  - `docs/07-开发与升级日志.md`

## 4. 回填模板（每批次结束必填）
### 4.1 结果
- 批次：`C?`
- 结论：`完成 / 部分完成 / 阻塞`
- 变更文件：
  - `...`

### 4.2 验证
- 主路径验证：`通过/失败`
- 兼容验证：`通过/失败`
- 异常验证：`通过/失败`

### 4.3 风险与下一步
- 当前风险：
  - `...`
- 下一批次入口：
  - `...`

## 5. 阻塞升级规则
- 同一批次阻塞超过 0.5 天，必须在 `docs/planning/00-规划迭代记录.md` 写明：
  - 阻塞原因
  - 已尝试方案
  - 需要决策项（是/否）
- 需要变更范围时，先更新 `01-实施清单` 再继续编码。
