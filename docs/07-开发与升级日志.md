# 开发与升级日志

## 2026-03-28（稳定标识匹配 + PowerShell兼容性增强）
- 备份索引与运行态合并逻辑升级：优先使用 `PNPDeviceID / HardwareID / USB(VID:PID+Serial)` 匹配，不再仅依赖打印机名称。
- 备份元数据与索引新增稳定标识字段，解决“系统改名后，已还原打印机被误判为待还原”问题。
- 打印机安装入口改为优先使用 `backupPrinterName` 作为索引查找键，避免显示名变化导致查找失败。
- 新增 `electron/powershell.mjs` 统一执行层：支持绝对路径候选、`-NonInteractive`、超时控制、错误收敛。
- 主进程与打印机状态 worker 全部切换到统一 PowerShell 执行层，降低不同 Windows 环境下的兼容性风险。
- 更新脚本签名 `sign.json`，确保脚本完整性校验可通过。
- 将 worker 目录规范化：`printer-state-worker.mjs` 移至 `electron/worker/`，为后续多 worker 扩展预留结构。
- PowerShell 模板后缀统一由 `*.ps1s` 改为 `*.scps1`，并同步调整脚本加载器与签名清单。

## 2026-03-28（本轮记忆补全）
- 新增 `docs/` 开发记忆库并分类整理。
- 将项目介绍、结构说明、注意事项、踩坑手册、协作约定、功能现状、总结落盘。
- 明确记录用户协作规则（仅用户要求时提交 git、commit 中文）。

## 近期关键里程碑（按提交记录整理）
- `80eefe3`：重构打印机运行时状态、资源与配置路径调整。
- `d9569ef`：脚本拆分、Pinia 运行时存储、启动流程优化。
- `81cf9fb`：托盘菜单、关闭主窗口改为隐藏。
- `f175012`：安装向导与并发互斥优化。
- `c471d1a`：USB 安装/卸载交互与自动同步增强。
- `b95e61b`：打印机备份、安装、卸载闭环。
- `11533d3`：打印机管理、系统设置、主题配置初步完成。

## 多轮迭代中的重点变更（非提交粒度）
- 安装向导 UI 持续打磨（样式、间距、文案、深浅主题适配、层级问题）。
- 打印机状态列从单纯占位到实时状态（就绪/脱机）展示。
- 打印机列表状态收敛到单一聚合对象 `PrinterServerManage`。
- USB 脱机判断改为多源融合（队列状态/WMI/PnP）。
- 虚拟打印机过滤回归修复，防止在聚合列表中回流。
- 设置路径由用户目录转向 `electron/resource/config/system.json`，并适配打包路径策略。

## 后续日志维护规范
- 新功能：写“做了什么 + 为什么这样做 + 影响范围”。
- 修 bug：写“复现条件 + 根因 + 修复点 + 防回归措施”。
- 每条日志建议包含日期，必要时附 commit id。
